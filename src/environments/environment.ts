/**
 * Development environment (default for `ng serve`). Production builds replace this file with
 * environment.prod.ts via the angular.json `fileReplacements` for the production configuration.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
};
